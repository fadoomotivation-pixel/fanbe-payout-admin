/// <reference types="vite/client" />

// Replaced at build time by the `define` in vite.config — the moment this bundle was
// built.  Shown on the Dashboard so a stale cached bundle can be told apart from a fix
// that did not work.
declare const __BUILD_STAMP__: string
